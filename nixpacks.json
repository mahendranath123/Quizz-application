{
  "phases": {
    "install": {
      "cmds": ["apt-get update && apt-get install -y ant openjdk-11-jdk"]
    },
    "build": {
      "cmds": ["ant"]
    },
    "start": {
      "cmds": ["java -jar path/to/your/output.jar"]
    }
  }
}
